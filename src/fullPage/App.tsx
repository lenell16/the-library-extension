import { BrowserRouter, Routes, Route } from 'react-router-dom';
// import { Popup } from '@/pages';

export default function App() {
  return (
    <BrowserRouter>
      <Routes>{/* <Route path="/" element={<Popup />} /> */}</Routes>
    </BrowserRouter>
  );
}
